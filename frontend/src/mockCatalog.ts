import { Item } from "./types";

/**
 * Catálogo de prueba vacío. 
 * Se eliminaron los productos 'MOCK' para obligar a la App a usar 
 * únicamente datos reales de Google Sheets o Firestore.
 */
export const MOCK_ITEMS: Item[] = [];
